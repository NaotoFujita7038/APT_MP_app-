<p><b>商品一覧</b>
全(<%=Item.count%>)件</p>
<% @items.each do |item| %>
  <div>
    <p>
    <%=link_to item_path(item.id) do %>
    <%= image_tag item.image, size: "100x200" if item.image.attached? %>
    <% end %>
    <p>商品名：<%=item.name%></p>
    <p>￥<%=item.price%></p>
  </div>
<% end %>

<p>操作や購入方法が分からない方はこちら</p>
<p><%=link_to new_consultation_form_path, class:"btn btn-outline-warning " do%>
   相談フォーム<% end %></p>
